import React from "react";
import * as SC from "./styles"
import { IUIProps } from "../../../commons/interfaces/UIProps";

export const Small = ({children}: IUIProps) => <SC.Small>{children}</SC.Small>